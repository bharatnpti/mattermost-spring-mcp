spring:
  ai:
    mcp:
      server:
        name: mattermost-mcp-server
        version: 1.0.0
        type: sync
        instructions: "This server mattermost apis"
        sse-message-endpoint: /mcp/messages
        capabilities:
          tool: true
          resource: true
          prompt: true
          completion: true
server:
  port: 8081

logging:
  level:
    org.springframework: TRACE
    io.modelcontextprotocol: TRACE
